# RelayGateway 基于 Sub2API 的改造计划

## 1. 目标与边界

以 Sub2API 为唯一应用基座，将对外品牌调整为 RelayGateway，并复用其现有的账号池、用户、API Key、分组倍率、用量、余额、兑换码、支付、监控和管理后台能力。

本阶段只做上线必需改造：品牌、用户文档、链动小铺入口、Resend 邮件配置和部署基线。内部包名、数据库表名、上游仓库标识不做机械替换；CPA 不再作为必需中间层，只有后续确认存在 Sub2API 无法覆盖的上游协议时才评估接入。

## 2. 已核实现状

- 技术栈：Go、Vue 3、PostgreSQL、Redis，官方提供 Docker Compose。
- 已有用户页面：仪表板、API Key、用量、监控、兑换、支付、推广。
- 已有管理能力：用户、账号池、分组、模型、倍率、兑换码、支付和站点设置。
- API Key 已支持绑定分组和额度；用量明细已展示倍率与计费结果。
- `site_name`、`site_logo`、`doc_url` 可在运行时配置，浏览器标题和 favicon 会同步更新。
- `custom_menu_items` 与自定义页面已支持运行时菜单、权限和 iframe 内容。
- 邮件模块已支持 SMTP、TLS、测试连接和测试邮件，可直接使用 Resend SMTP。
- 当前本地目录为 `E:\ai\dev\sub2api`，服务计划监听 `127.0.0.1:8081`。

## 3. 架构决策

1. **配置优先**：能通过后台设置完成的品牌、文档链接、菜单和 SMTP 配置，不修改源码。
2. **最小分叉**：只维护 RelayGateway 特有的小补丁，便于持续合并 Wei-Shaw/sub2api 上游更新。
3. **Sub2API 直接承载网关**：用户调用 Sub2API 暴露的 OpenAI/Claude 等兼容端点，不额外增加 CPA 跳转层。
4. **倍率沿用原生模型**：后台配置账号、分组与倍率，用户端沿用现有 `Rate Multiplier` 和账单字段；仅补齐中文文案时改前端。
5. **链动小铺先用自定义菜单**：店铺 URL 由 `custom_menu_items` 运行时配置；iframe 被对方安全策略禁止时提供新窗口访问，不新增支付后端。
6. **密钥只进环境或后台密文配置**：`.env` 不提交 Git；生产环境使用服务器 Secret 文件或容器 Secret 注入。

## 4. 任务清单

### 阶段 A：本地基线

#### A1. 启动官方 Compose 服务

**说明**：释放 Docker 所需磁盘空间后，使用现有 `deploy/.env` 启动 PostgreSQL、Redis 和 Sub2API。

**验收标准**：

- [ ] `docker compose ps` 中三个服务均为运行或健康状态。
- [ ] `http://127.0.0.1:8081/health` 返回成功。
- [ ] 浏览器可打开首页并使用初始管理员登录。

**验证**：`docker compose logs --tail 100 sub2api` 无持续错误；完成一次登录冒烟测试。

**依赖**：释放 C 盘空间并恢复 Docker Desktop。

**预计范围**：XS，仅本地运行配置。

#### A2. 建立未改造功能基线

**说明**：记录当前版本、容器镜像、默认页面和核心流程，避免后续将上游原有问题误认为改造回归。

**验收标准**：

- [ ] 管理员可创建分组、添加一个测试账号并查看状态。
- [ ] 普通用户可创建 API Key、查看倍率与用量页面。
- [ ] 兑换码生成和兑换流程完成一次冒烟测试。

**验证**：保留脱敏后的测试记录与关键日志，不保存真实上游密钥。

**依赖**：A1。

**预计范围**：XS，无代码改动。

### 检查点 A

- [ ] 本地服务稳定启动，核心原生流程可用。
- [ ] 对后续改动建立可重复的冒烟测试清单。

### 阶段 B：零代码品牌与服务配置

#### B1. 配置 RelayGateway 品牌

**说明**：通过管理后台设置站点名、Logo、favicon、站点副标题、联系信息和 API Base URL。

**验收标准**：

- [ ] 登录页、首页、用户后台、管理后台和浏览器标题显示 RelayGateway。
- [ ] 页面不再向普通用户展示 New API 字样。
- [ ] Sub2API 的许可证和必要归属信息保留在发行材料中。

**验证**：桌面与移动宽度各检查登录页、仪表板和 API Key 页面。

**依赖**：A1。

**预计范围**：XS，后台配置；仅缺失处再补前端。

#### B2. 配置 Resend SMTP

**说明**：复用 Sub2API 邮件模块，配置 Resend SMTP、`sys@relay-gateway.xyz` 发件地址和 RelayGateway 发件名称。

**验收标准**：

- [ ] 后台 SMTP 连接测试成功。
- [ ] 指定测试邮箱能收到验证码和密码重置邮件。
- [ ] 邮件主题与正文使用 RelayGateway 站点名。

**验证**：后台发送测试邮件，并完成一次注册验证码和忘记密码冒烟测试。

**依赖**：B1；需要 Resend API Key 作为 SMTP 密码。

**预计范围**：XS，后台配置。

#### B3. 配置模型分组与倍率样例

**说明**：使用现有账号、模型、分组和倍率配置一条可计费测试链路，确认用户端显示如 `0.18x` 的倍率。

**验收标准**：

- [ ] 测试 API Key 只能选择一个目标分组。
- [ ] 分组包含管理员指定模型并应用配置倍率。
- [ ] 用量明细同时记录 Token、倍率和扣费。

**验证**：发送一次非流式和一次流式请求，对照管理后台与用户用量记录。

**依赖**：A2；需要一组合法测试上游凭据。

**预计范围**：S，优先仅配置。

### 检查点 B

- [ ] 品牌、邮件和一条模型计费链路从前端到后端均可用。
- [ ] 如原生倍率文案已满足需求，不创建重复组件。

### 阶段 C：用户文档

#### C1. 编写 RelayGateway 用户操作文档

**说明**：编写面向普通用户的中文文档，覆盖注册登录、余额与兑换、创建 API Key、Base URL、分组模型、倍率、额度、用量排查和密钥安全。

**验收标准**：

- [ ] 文档中的页面名称和实际 UI 一致。
- [ ] 给出 OpenAI SDK、Codex CLI、Claude Code 的最小可运行配置示例。
- [ ] 示例只使用占位密钥，不包含真实 Secret。

**验证**：按文档使用新用户完成“注册 → 兑换 → 创建 Key → 调用 → 查用量”冒烟流程。

**依赖**：B1、B3。

**预计范围**：S，1 个文档文件。

#### C2. 接入文档入口

**说明**：将 `doc_url` 指向 RelayGateway 用户文档，核对登录页、首页和后台导航中的文档入口。

**验收标准**：

- [ ] 所有面向用户的文档入口打开 RelayGateway 文档。
- [ ] 不再跳转到 New API 或无关产品文档。
- [ ] 外链使用 HTTPS 且新窗口行为一致。

**验证**：浏览器点击所有可见文档入口。

**依赖**：C1。

**预计范围**：XS，后台配置；缺失入口最多修改 1 至 2 个前端文件。

### 阶段 D：链动小铺

#### D1. 使用自定义菜单配置“支付 → 链动小铺”

**说明**：用 `custom_menu_items` 添加用户可见菜单，店铺地址为运行时配置，不在源码中硬编码。

**验收标准**：

- [ ] 普通用户侧边栏显示“支付 → 链动小铺”，管理员菜单不被破坏。
- [ ] 菜单顺序和各页面侧边栏保持一致。
- [ ] 修改后台 URL 后无需重新构建即可生效。

**验证**：普通用户与管理员各登录一次，检查导航、刷新和移动端折叠菜单。

**依赖**：A2；需要链动小铺正式店铺 URL。

**预计范围**：XS，后台配置。

#### D2. 验证内嵌与降级访问

**说明**：验证链动小铺是否允许 iframe；若禁止嵌入，使用现有自定义菜单外链或最小增加“新窗口打开”降级按钮。

**验收标准**：

- [ ] 允许 iframe 时可在站内正常浏览商品并滚动。
- [ ] 禁止 iframe 或加载超时时，用户能明确地在新窗口打开店铺。
- [ ] 不绕过对方的 CSP、X-Frame-Options 或登录安全策略。

**验证**：浏览器网络与控制台无未处理异常，桌面和移动端各冒烟一次。

**依赖**：D1。

**预计范围**：S，优先零代码，必要时最多 2 个前端文件。

#### D3. 在兑换页增加购买入口

**说明**：在现有兑换页增加“购买兑换码”按钮，复用 D1 的同一运行时 URL 配置，避免维护两个店铺地址。

**验收标准**：

- [ ] 店铺 URL 已配置时显示按钮并正确跳转。
- [ ] 未配置时不显示无效按钮。
- [ ] 原兑换提交、错误提示和成功刷新余额流程不受影响。

**验证**：为 URL 已配置和未配置两种状态各运行一个前端测试，并完成浏览器冒烟测试。

**依赖**：D1。

**预计范围**：S，1 至 2 个前端文件。

### 检查点 D

- [ ] 用户可从导航或兑换页进入链动小铺。
- [ ] 店铺不可内嵌时仍有可用且明确的访问路径。

### 阶段 E：上线准备

#### E1. 固化部署与备份基线

**说明**：为腾讯云新加坡单机部署固定镜像版本，配置 HTTPS 反向代理、PostgreSQL 定时备份和 Redis 持久化策略。

**验收标准**：

- [ ] 镜像不使用不可追踪的漂移版本上线。
- [ ] 数据库每日备份、保留 7 天，并完成一次恢复演练。
- [ ] `.env`、备份和日志权限最小化，均不进入 Git。

**验证**：在空数据库实例执行一次备份恢复；检查 HTTPS、健康检查和容器重启恢复。

**依赖**：A2。

**预计范围**：M，部署配置与运维文档。

#### E2. 验证国内访问链路

**说明**：网站与 API 先统一部署在新加坡，通过真实大陆网络测量延迟、流式稳定性和 DNS/HTTPS 可达性；只有数据证明需要时再增加大陆 API 接入层。

**验收标准**：

- [ ] 大陆网络可完成登录、兑换、创建 Key 和流式 API 调用。
- [ ] 记录连接、首 Token、总耗时和失败率。
- [ ] 若不达标，形成大陆接入层的合规、网络和故障切换方案后再实施。

**验证**：至少两个大陆运营商网络完成固定请求集冒烟测试。

**依赖**：E1、B3。

**预计范围**：S，测试与决策记录。

#### E3. 上线前安全与合规检查

**说明**：检查上游账号授权、日志脱敏、Secret、注册风控、倍率计费和 LGPL-3.0 分发义务。

**验收标准**：

- [ ] 日志和错误响应不泄露 API Key、Cookie、Token 或 SMTP 密码。
- [ ] 只接入允许转售或代理调用的上游资源。
- [ ] 发布材料满足 LGPL-3.0，并保留上游版权与许可证。

**验证**：运行现有后端测试、前端 `typecheck`、`lint:check`、`test:run`、`build`，再完成核心用户流程冒烟测试。

**依赖**：B 至 E 阶段全部任务。

**预计范围**：M，检查和必要的小修复。

## 5. 每个开发任务的完成标准

- 改动只覆盖该任务验收范围，不顺带重构无关代码。
- 优先复用 Sub2API 现有设置、组件、API 和测试设施。
- 新增或修改的函数按项目约定写简短中文注释，说明用途、入参和出参。
- 后端功能必须同步完成对应前端联调；每个任务至少完成一次核心路径冒烟测试。
- 不提交真实密钥、JWT、用户数据、`.env`、数据库或日志。
- 相关测试、类型检查和构建通过；无法执行的检查必须记录原因。

## 6. 风险与应对

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| 链动小铺禁止 iframe | 站内商品页空白 | 不绕过安全策略，自动或明确提供新窗口入口 |
| 上游 Sub2API 快速更新 | 私有分叉难合并 | 配置优先、缩小补丁、记录上游版本 |
| 订阅账号条款不允许转售 | 封号、退款或法律风险 | 上线前逐供应源核验授权范围，不合规资源不进入付费目录 |
| 新加坡链路在大陆波动 | 请求延迟或中断 | 先实测；达到阈值后再设计合规的大陆接入层 |
| 金额和 Token 结算偏差 | 用户争议 | 使用原生账单记录做对账测试，倍率变更保留审计记录 |
| 单机故障 | 网站与 API 同时不可用 | 健康检查、自动重启、异机备份和定期恢复演练 |

## 7. 实施前待提供或确认

1. RelayGateway Logo 和 favicon；没有时可先用纯文字品牌。
2. RelayGateway 用户文档最终托管地址；开发期可先放仓库文档。
3. 链动小铺正式店铺 URL，并确认是否允许 iframe 嵌入。
4. Resend API Key 仅在本地或服务器 Secret 中注入，不写入文档或 Git。
5. 用于冒烟测试的、明确允许当前用途的上游账号或 API Key。

## 8. 明确不做

- 不删除或覆盖现有 `new-api`、`relay-gateway` 项目。
- 不批量替换源码中的 Sub2API 内部标识。
- 不重复开发已有用户、密钥、计费、兑换码、账号池或监控模块。
- 不在未实测前建设额外 CPA 或大陆中转层。
- 不绕过链动小铺或上游服务的安全与授权限制。
